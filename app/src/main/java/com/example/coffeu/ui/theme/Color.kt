package com.example.coffeu.ui.theme

import androidx.compose.ui.graphics.Color
// Colores Primarios (Botones principales, elementos destacados)
val PrimaryNormal = Color(0xFFEF6820) // Botones primarios, elementos activos
val PrimaryLightActive = Color(0xFFFF692E) // Botones primarios en estado activo
val PrimaryLightHover = Color(0xFFFF6B0E) // Botones primarios en hover (para web)
val PrimaryLight = Color(0xFFFF982E) // Variante clara del color primario

// Colores de Texto y Etiquetas
val LabelPrimary = Color(0xFF1F1F1F) // Texto principal en fondos oscuros
val LabelSecondary = Color(0xFF6B7586) // Texto secundario, subtítulos
val LabelTertiary = Color(0xFFFFFFFF) // Texto terciario, deshabilitado

// Fondos del Sistema
val BackgroundPrimary = Color(0xFFA7AEC1) // Fondo principal de pantallas
val BackgroundSecondary = Color(0xFFC5CED2) // Fondo de tarjetas, elementos secundarios
val BackgroundTertiary = Color(0xFFF5F5F5) // Fondo terciario, áreas de separación
val BackgroundQuaternary = Color(0xFFFBFBFD) // Fondo cuaternario, muy claro

// Colores de Acción y Estado
val SystemRed = Color(0xFFEF5C5C) // Errores, acciones destructivas
val SystemOrange = Color(0xFFFF9900) // Advertencias, alertas
val SystemGreen = Color(0xFF00D261) // Éxito, confirmaciones
val SystemBlue = Color(0xFF007AFF) // Enlaces, información, acciones neutras